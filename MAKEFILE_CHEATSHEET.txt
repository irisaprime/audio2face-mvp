╔═══════════════════════════════════════════════════════════════════════╗
║                  AUDIO2FACE MVP - MAKEFILE CHEATSHEET                 ║
╚═══════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────┐
│ QUICK START (First Time)                                            │
└─────────────────────────────────────────────────────────────────────┘

  1️⃣  make check          # Verify requirements
  2️⃣  make install        # Install dependencies
  3️⃣  make generate-test  # Create test audio
  4️⃣  make run            # Start application

  OR: make setup-all      # All-in-one setup command


┌─────────────────────────────────────────────────────────────────────┐
│ DAILY USAGE                                                          │
└─────────────────────────────────────────────────────────────────────┘

  ▶️  make run            # Start backend + frontend
  🔍 make status         # Check project status
  🌐 make open           # Open browser
  ⏹️  make stop           # Stop all services


┌─────────────────────────────────────────────────────────────────────┐
│ SETUP COMMANDS                                                       │
└─────────────────────────────────────────────────────────────────────┘

  make check             Check system requirements
  make install           Install Python dependencies
  make setup-sdk         Setup Audio2Face SDK (GPU required)
  make setup-all         Complete setup (check + install + test)
  make install-system-deps  Install system packages (sudo required)


┌─────────────────────────────────────────────────────────────────────┐
│ RUN COMMANDS                                                         │
└─────────────────────────────────────────────────────────────────────┘

  make run               Start backend + frontend (uses tmux)
  make run-backend       Start backend only (port 8000)
  make run-frontend      Start frontend only (port 3000)
  make dev-backend       Backend with auto-reload (requires entr)
  make stop              Stop all services


┌─────────────────────────────────────────────────────────────────────┐
│ TESTING COMMANDS                                                     │
└─────────────────────────────────────────────────────────────────────┘

  make generate-test     Generate test audio files
  make test              Run API tests
  make quick-test        Quick test with sample audio


┌─────────────────────────────────────────────────────────────────────┐
│ UTILITY COMMANDS                                                     │
└─────────────────────────────────────────────────────────────────────┘

  make status            Show project status
  make clean             Clean temp files
  make clean-all         Clean everything (including venv)
  make logs-backend      View backend logs (tmux)
  make logs-frontend     View frontend logs (tmux)
  make open              Open browser to frontend
  make list              List all available commands
  make help              Show detailed help


┌─────────────────────────────────────────────────────────────────────┐
│ COMMON WORKFLOWS                                                     │
└─────────────────────────────────────────────────────────────────────┘

  🔧 DEVELOPMENT WORKFLOW
     Terminal 1: make dev-backend    # Auto-reload on changes
     Terminal 2: make run-frontend   # Serve frontend

  🧪 TESTING WORKFLOW
     make run-backend                # Start backend
     make test                       # Run tests

  🔄 RESET EVERYTHING
     make stop                       # Stop services
     make clean-all                  # Clean everything
     make setup-all                  # Reinstall
     make run                        # Start fresh


┌─────────────────────────────────────────────────────────────────────┐
│ URL QUICK REFERENCE                                                  │
└─────────────────────────────────────────────────────────────────────┘

  Frontend:     http://localhost:3000
  Backend API:  http://localhost:8000
  API Docs:     http://localhost:8000/docs
  Health:       http://localhost:8000/health


┌─────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                      │
└─────────────────────────────────────────────────────────────────────┘

  ❌ "Virtual environment not found"
     → make install

  ❌ "Backend not running" during tests
     → make run-backend

  ❌ Port already in use
     → make stop

  ❌ Something broken
     → make clean-all && make setup-all


┌─────────────────────────────────────────────────────────────────────┐
│ TIPS & TRICKS                                                        │
└─────────────────────────────────────────────────────────────────────┘

  💡 Always run 'make status' to check project state
  💡 Use 'make run' with tmux for easy parallel execution
  💡 Use 'make dev-backend' for development (auto-reload)
  💡 Run 'make help' for detailed command descriptions
  💡 Check 'make list' to see all available targets


┌─────────────────────────────────────────────────────────────────────┐
│ WHEN GPU IS AVAILABLE                                                │
└─────────────────────────────────────────────────────────────────────┘

  1. make check                      # Should show CUDA
  2. make setup-sdk                  # Build SDK (~10 min)
  3. Download model from HuggingFace # Manual step
  4. Download avatar from RPM        # Manual step
  5. make status                     # Verify all ready
  6. make run                        # Launch!


╔═══════════════════════════════════════════════════════════════════════╗
║  📖 For detailed help: MAKEFILE_GUIDE.md                              ║
║  📖 For full docs: README.md                                          ║
║  📖 For GPU setup: GPU_SETUP_GUIDE.md                                 ║
╚═══════════════════════════════════════════════════════════════════════╝
